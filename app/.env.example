# Frontend API base URL used by axios calls.
# Local dev default: /api (proxied to backend by Vite)
VITE_API_BASE_URL=/api

# Backend URL used by the Vite dev proxy
VITE_API_PROXY_TARGET=http://localhost:3001
